==================
Select 1
==================

SELECT 1;

---

(program
  (statement
   (select_statement
    (select
     (keyword_select)
     (select_expression)))))

==================
Simple select
==================

SELECT * FROM my_table;

---

(program
  (statement
   (select_statement
    (select
     (keyword_select)
     (select_expression))
    (from
     (keyword_from)
     (table_expression (table_name))))))
