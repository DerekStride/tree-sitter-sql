================================================================================
JSON traversal
================================================================================

SELECT 
    "user"->>'login',
    "user"->'adress' ->> 'city',
    "user" #> 'items',
    "user" #>> 'more_items'
AS username FROM users;

--------------------------------------------------------------------------------

(program
 (statement
  (select
   (keyword_select)
   (select_expression
    (term
     (json_traversal
      (literal))
     (keyword_as)
     (identifier))))
  (from
   (keyword_from)
   (relation
    (object_reference
     (identifier))))))
