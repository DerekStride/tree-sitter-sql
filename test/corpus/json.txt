================================================================================
JSON traversal
================================================================================

SELECT
    "user"->>'login' as login,
    "user"->'address' ->> 'city',
    "user"->'address' ->> 'state' as userstate,
    "user" #> 'items',
    "user" #>> 'more_items' AS more
FROM users;

--------------------------------------------------------------------------------

(program
 (statement
  (select
   (keyword_select)
   (select_expression
    (term
     (json_traversal
      (literal))
     (keyword_as)
     (identifier))
    (term
     (json_traversal
      (json_traversal
       (literal))))
    (term
     (json_traversal
      (json_traversal
       (literal)))
     (keyword_as)
     (identifier))
    (term
     (json_traversal
      (literal)))
    (term
     (json_traversal
      (literal))
      (keyword_as)
      (identifier))))
    (from
     (keyword_from)
     (relation
      (object_reference
       (identifier))))))
